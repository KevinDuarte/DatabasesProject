SELECT E.name;
FROM event E;
WHERE pubprirso = '0';

SELECT E.name;
FROM event E, RSO R;
WHERE pubprirso = '2' AND eventID = rsoID;

SELECT E.name;
FROM event E, location L;
WHERE pubprirso = '0' ; 

SELECT E.name;
FROM event E, location L;
WHERE pubprirso = '0' ;

SELECT E.name;
FROM event E, university U;
WHERE pubprirso = '0' AND universityID = eventID;  

INSERTS INTO student (name, email, university)
VALUES()

INSERTS INTO event (eventID, type, name, date, pubprirso)
VALUES()

INSERTS INTO rso (rsoID, name)
VALUES()
